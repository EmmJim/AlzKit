

const initialState = {
    user: null,
    error: null,
    loading: false,
    screen: null
}

export const authReducer = (state = initialState, action) => {
    switch(action.type){
        
        default:
            return state;
    }
};